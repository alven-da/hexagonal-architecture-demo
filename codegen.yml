overwrite: true
schema: 'http://localhost:3000'
documents: null
generates:
  'src/interface/graphql/generated-graphql.ts':
    plugins:
      - 'typescript'
      - 'typescript-resolvers'
      - 'typescript-document-nodes'
    config:
      namingConvention:
        enumValues: keep
